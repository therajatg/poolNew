{
    "version": 2,
    "builds": [
        {
            "src": "src/index.js",
            "use": "@vercel/node",
            "buildCommand": "npm install --legacy-peer-deps && npm audit fix --force"
        }
    ]
}